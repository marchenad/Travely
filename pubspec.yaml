name: travely
description: "Travely Versión 1.1"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.2.0
  latlong2: ^0.9.0
  geolocator: ^10.1.0
  cupertino_icons: ^1.0.8
  postgres: ^3.0.0
  http: ^1.1.0
  image_picker: ^1.0.7
  firebase_core: ^4.5.0
  firebase_messaging: ^16.1.2
  shared_preferences: ^2.2.2
  flutter_tts: ^4.2.2
  socket_io_client: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
